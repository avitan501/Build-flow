"use client"

import { AlertTriangle, CheckCircle2, ExternalLink, FileSearch, LoaderCircle, RotateCw, Route, Save, ShieldCheck } from "lucide-react"
import Image from "next/image"
import Link from "next/link"
import { useRouter } from "next/navigation"
import { useState, useTransition } from "react"

import { approveManagerDocumentAction, retryManagerDocumentExtractionAction, routeManagerDocumentToSupplierPricingAction, saveManagerDocumentReviewAction } from "@/app/admin/documents/actions"
import { confidenceLabel, managerDocumentTypeLabel, managerDocumentTypes, type ManagerDocumentItemRecord, type ManagerDocumentRecord } from "@/lib/manager-documents"

function inputMoney(value: number | null) { return value === null ? "" : String(value) }
function numberOrNull(value: string) { const parsed = Number(value); return value.trim() && Number.isFinite(parsed) && parsed >= 0 ? parsed : null }

export function ManagerDocumentReview({ document, items, documentUrl, departments, canApprove }: { document: ManagerDocumentRecord; items: ManagerDocumentItemRecord[]; documentUrl: string; departments: string[]; canApprove: boolean }) {
  const router = useRouter()
  const [draft, setDraft] = useState(document)
  const [lines, setLines] = useState(items)
  const [acknowledgeWarnings, setAcknowledgeWarnings] = useState(false)
  const [feedback, setFeedback] = useState("")
  const [error, setError] = useState("")
  const [pending, startTransition] = useTransition()
  const supplierDocument = ["supplier_quote", "supplier_invoice", "receipt", "catalog_price_list", "purchase_order"].includes(draft.document_type)
  const requiresPricing = ["supplier_quote", "supplier_invoice", "receipt", "catalog_price_list", "client_estimate", "purchase_order"].includes(draft.document_type)
  const allSelectedValid = lines.filter((line) => line.selected).every((line) => line.validation_status === "valid")

  function run(work: () => Promise<void>) { setError(""); setFeedback(""); startTransition(work) }
  function save() {
    run(async () => {
      const result = await saveManagerDocumentReviewAction({ documentId: document.id, documentType: draft.document_type, title: draft.title, partyName: draft.party_name, documentNumber: draft.document_number, documentDate: draft.document_date || "", dueDate: draft.due_date || "", expiresOn: draft.expires_on || "", department: draft.department, subtotal: draft.subtotal, discount: draft.discount, deliveryCharge: draft.delivery_charge, taxAmount: draft.tax_amount, taxPercent: draft.tax_percent, total: draft.total, acknowledgeWarnings, evidence: draft.evidence, items: lines.map((line) => ({ id: line.id, description: line.description, itemCode: line.item_code, specification: line.specification, quantity: line.quantity, unit: line.unit, unitPrice: line.unit_price, lineTotal: line.line_total, selected: line.selected })) })
      if (!result.ok) { setError(result.error); return }
      setFeedback(result.message); router.refresh()
    })
  }
  function approve() { run(async () => { const result = await approveManagerDocumentAction(document.id); if (!result.ok) { setError(result.error); return }; setFeedback(result.message); router.refresh() }) }
  function retry() {
    const replaceExisting = lines.length > 0
    if (replaceExisting && !window.confirm("Re-read with AI and replace the current extracted lines? The original document will stay unchanged.")) return
    run(async () => { const result = await retryManagerDocumentExtractionAction(document.id, replaceExisting); if (!result.ok) { setError(result.error); return }; setFeedback(result.message); router.refresh() })
  }
  function routeSupplier() { run(async () => { const result = await routeManagerDocumentToSupplierPricingAction(document.id); if (!result.ok) { setError(result.error); return }; router.push(`/admin/supplier-quotes/${result.data.quoteId}`) }) }
  function updateLine(index: number, changes: Partial<ManagerDocumentItemRecord>) {
    setLines((current) => current.map((line, lineIndex) => {
      if (lineIndex !== index) return line
      const next = { ...line, ...changes }
      const expected = next.quantity !== null && next.unit_price !== null ? Math.round(next.quantity * next.unit_price * 100) / 100 : null
      const mismatch = expected !== null && next.line_total !== null && Math.abs(expected - next.line_total) > 0.03
      const incomplete = next.selected && (!next.description.trim() || next.quantity === null || !next.unit.trim() || (requiresPricing && (next.unit_price === null || next.line_total === null)))
      return { ...next, validation_status: mismatch ? "mismatch" : incomplete ? "needs_review" : "valid" }
    }))
  }

  return <div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_22rem]">
    <div className="min-w-0 space-y-5">
      {document.warnings.length ? <section className="border border-amber-200 bg-amber-50 p-4" aria-labelledby="document-warnings-heading"><div className="flex items-center gap-2"><AlertTriangle className="h-5 w-5 text-amber-700" /><h2 id="document-warnings-heading" className="font-bold text-amber-950">Check before approval</h2></div><ul className="mt-3 space-y-2 text-sm leading-5 text-amber-950">{document.warnings.map((warning) => <li key={warning}>• {warning}</li>)}</ul><label className="mt-4 flex items-start gap-2 text-sm font-semibold text-amber-950"><input type="checkbox" checked={acknowledgeWarnings} onChange={(event) => setAcknowledgeWarnings(event.target.checked)} className="mt-0.5 h-4 w-4 accent-amber-700" />I compared these warnings with the original and want to save my corrected values.</label></section> : <div className="flex items-center gap-2 border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-900"><CheckCircle2 className="h-4 w-4" />No calculation warning is currently open. Human approval is still required.</div>}

      <section className="border border-slate-200 bg-white shadow-sm" aria-labelledby="document-details-heading"><div className="border-b border-slate-200 px-4 py-4 sm:px-5"><h2 id="document-details-heading" className="text-lg font-bold">Document details</h2><p className="mt-1 text-xs text-slate-500">AI suggestions are editable. Blank is safer than guessing.</p></div><div className="grid gap-4 p-4 sm:grid-cols-2 sm:p-5">
        <label className="grid gap-1 text-xs font-bold text-slate-600">Document type<select value={draft.document_type} onChange={(event) => setDraft({ ...draft, document_type: event.target.value as ManagerDocumentRecord["document_type"] })} className="min-h-11 rounded-lg border border-slate-300 bg-white px-3 text-sm font-medium text-slate-950">{managerDocumentTypes.map((type) => <option key={type} value={type}>{managerDocumentTypeLabel(type)}</option>)}</select></label>
        <label className="grid gap-1 text-xs font-bold text-slate-600">Title<input value={draft.title} onChange={(event) => setDraft({ ...draft, title: event.target.value })} className="min-h-11 rounded-lg border border-slate-300 px-3 text-sm font-medium text-slate-950" /></label>
        <label className="grid gap-1 text-xs font-bold text-slate-600">Vendor / customer / party<input value={draft.party_name} onChange={(event) => setDraft({ ...draft, party_name: event.target.value })} className="min-h-11 rounded-lg border border-slate-300 px-3 text-sm font-medium text-slate-950" /></label>
        <label className="grid gap-1 text-xs font-bold text-slate-600">Document number<input value={draft.document_number} onChange={(event) => setDraft({ ...draft, document_number: event.target.value })} className="min-h-11 rounded-lg border border-slate-300 px-3 text-sm font-medium text-slate-950" /></label>
        <label className="grid gap-1 text-xs font-bold text-slate-600">Document date<input type="date" value={draft.document_date || ""} onChange={(event) => setDraft({ ...draft, document_date: event.target.value || null })} className="min-h-11 rounded-lg border border-slate-300 px-3 text-sm" /></label>
        <label className="grid gap-1 text-xs font-bold text-slate-600">Due date<input type="date" value={draft.due_date || ""} onChange={(event) => setDraft({ ...draft, due_date: event.target.value || null })} className="min-h-11 rounded-lg border border-slate-300 px-3 text-sm" /></label>
        <label className="grid gap-1 text-xs font-bold text-slate-600">Expires on<input type="date" value={draft.expires_on || ""} onChange={(event) => setDraft({ ...draft, expires_on: event.target.value || null })} className="min-h-11 rounded-lg border border-slate-300 px-3 text-sm" /></label>
        <label className="grid gap-1 text-xs font-bold text-slate-600">Department<select value={draft.department} onChange={(event) => setDraft({ ...draft, department: event.target.value })} className="min-h-11 rounded-lg border border-slate-300 bg-white px-3 text-sm">{departments.map((department) => <option key={department}>{department}</option>)}</select></label>
      </div></section>

      <section className="border border-slate-200 bg-white shadow-sm" aria-labelledby="document-lines-heading"><div className="flex items-center justify-between border-b border-slate-200 px-4 py-4 sm:px-5"><div><h2 id="document-lines-heading" className="text-lg font-bold">Extracted lines</h2><p className="mt-1 text-xs text-slate-500">Each line keeps page, source text, confidence, and math status.</p></div><span className="text-xs font-bold text-slate-500">{lines.length} lines</span></div>{lines.length ? <div className="overflow-x-auto"><table className="w-full min-w-[68rem] border-collapse text-left text-xs"><thead className="bg-slate-950 text-white"><tr><th className="px-3 py-3">Use</th><th className="min-w-56 px-3 py-3">Item</th><th className="px-3 py-3">Qty</th><th className="px-3 py-3">Unit</th><th className="px-3 py-3">Unit price</th><th className="px-3 py-3">Line total</th><th className="min-w-48 px-3 py-3">Evidence</th><th className="px-3 py-3">Check</th></tr></thead><tbody className="divide-y divide-slate-200">{lines.map((line, index) => <tr key={line.id} className="align-top"><td className="px-3 py-3"><input type="checkbox" checked={line.selected} onChange={(event) => updateLine(index, { selected: event.target.checked })} className="h-4 w-4 accent-[#0071e3]" /></td><td className="px-3 py-3"><input value={line.description} onChange={(event) => updateLine(index, { description: event.target.value })} className="min-h-9 w-full rounded border border-slate-300 px-2 font-semibold" /><input value={line.specification} onChange={(event) => updateLine(index, { specification: event.target.value })} placeholder="Specification" className="mt-1 min-h-9 w-full rounded border border-slate-200 px-2 text-slate-600" /></td><td className="px-3 py-3"><input type="number" min="0.001" step="0.001" value={line.quantity ?? ""} onChange={(event) => updateLine(index, { quantity: numberOrNull(event.target.value) })} className="h-9 w-20 rounded border border-slate-300 px-2" /></td><td className="px-3 py-3"><input value={line.unit} onChange={(event) => updateLine(index, { unit: event.target.value })} className="h-9 w-20 rounded border border-slate-300 px-2" /></td><td className="px-3 py-3"><input type="number" min="0" step="0.01" value={line.unit_price ?? ""} onChange={(event) => updateLine(index, { unit_price: numberOrNull(event.target.value) })} className="h-9 w-24 rounded border border-slate-300 px-2" /></td><td className="px-3 py-3"><input type="number" min="0" step="0.01" value={line.line_total ?? ""} onChange={(event) => updateLine(index, { line_total: numberOrNull(event.target.value) })} className="h-9 w-24 rounded border border-slate-300 px-2" /></td><td className="px-3 py-3 text-slate-600"><p>Page {line.source_page ?? "?"} · {confidenceLabel(line.confidence)}</p><p className="mt-1 line-clamp-3 leading-4">{line.source_text || "No source snippet"}</p></td><td className="px-3 py-3"><span className={`inline-flex rounded-full px-2 py-1 font-bold ${line.validation_status === "valid" ? "bg-emerald-50 text-emerald-800" : line.validation_status === "mismatch" ? "bg-rose-50 text-rose-800" : "bg-amber-50 text-amber-800"}`}>{line.validation_status === "valid" ? "Ready" : line.validation_status === "mismatch" ? "Math mismatch" : "Review"}</span></td></tr>)}</tbody></table></div> : <div className="px-5 py-10 text-center"><FileSearch className="mx-auto h-8 w-8 text-slate-300" /><p className="mt-3 font-bold">No dependable lines yet</p><button type="button" onClick={retry} disabled={pending} className="mt-4 inline-flex min-h-10 items-center gap-2 rounded-lg bg-[#0071e3] px-4 text-sm font-bold text-white"><RotateCw className="h-4 w-4" />Re-read with AI</button></div>}</section>

      <section className="border border-slate-200 bg-white p-4 shadow-sm sm:p-5" aria-labelledby="document-totals-heading"><h2 id="document-totals-heading" className="text-lg font-bold">Totals check</h2><div className="mt-4 grid gap-3 sm:grid-cols-3">{[["Subtotal", "subtotal"], ["Discount", "discount"], ["Delivery / freight", "delivery_charge"], ["Tax amount", "tax_amount"], ["Tax %", "tax_percent"], ["Total", "total"]].map(([label, key]) => <label key={key} className="grid gap-1 text-xs font-bold text-slate-600">{label}<input type="number" min="0" step={key === "tax_percent" ? "0.0001" : "0.01"} value={inputMoney(draft[key as keyof ManagerDocumentRecord] as number | null)} onChange={(event) => setDraft({ ...draft, [key]: numberOrNull(event.target.value) ?? (key === "discount" || key === "delivery_charge" ? 0 : null) })} className="min-h-10 rounded-lg border border-slate-300 px-3 text-sm text-slate-950" /></label>)}</div></section>

      {draft.evidence.length ? <details open className="border border-slate-200 bg-white shadow-sm"><summary className="cursor-pointer px-4 py-4 font-bold sm:px-5">Sources and field evidence ({draft.evidence.length})</summary><div className="border-t border-slate-200 bg-slate-50 px-4 py-3 text-xs font-semibold text-slate-700 sm:px-5">Source: {document.source_label} · {document.file_name}{document.source_reference ? ` · ${document.source_reference}` : ""}</div><div className="divide-y divide-slate-200">{draft.evidence.map((evidence, index) => <div key={`${evidence.field}-${index}`} className={`grid gap-2 px-4 py-3 text-sm sm:grid-cols-[10rem_minmax(0,1fr)_auto] sm:px-5 ${evidence.selected !== false ? "" : "bg-slate-50 opacity-60"}`}><span className="font-bold">{evidence.field}</span><span><span className="font-semibold">{evidence.value}</span><span className="mt-1 block text-xs text-slate-500">{evidence.sourceText}</span></span><span className="flex items-center gap-2 text-xs font-semibold text-slate-500"><span>Page {evidence.page ?? "?"} · {confidenceLabel(evidence.confidence)}</span><button type="button" onClick={() => setDraft({ ...draft, evidence: draft.evidence.map((entry, evidenceIndex) => evidenceIndex === index ? { ...entry, selected: entry.selected === false } : entry) })} className="min-h-8 rounded-md border border-slate-300 bg-white px-2 font-bold text-slate-700">{evidence.selected !== false ? "Ignore source" : "Use source"}</button></span></div>)}</div></details> : null}
    </div>

    <aside className="space-y-4 xl:sticky xl:top-5 xl:self-start">
      <section className="border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-[#0071e3]" /><h2 className="font-bold">Review control</h2></div><p className="mt-2 text-xs leading-5 text-slate-500">AI prepares; you decide. Saving corrections never changes the original file.</p>{!canApprove ? <p className="mt-3 rounded-lg border border-sky-200 bg-sky-50 p-3 text-xs font-semibold text-sky-900">Approved manager access is required before routing financial information.</p> : null}{error ? <p role="alert" className="mt-3 rounded-lg border border-rose-200 bg-rose-50 p-3 text-xs font-semibold text-rose-700">{error}</p> : null}{feedback ? <p role="status" className="mt-3 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-xs font-semibold text-emerald-800">{feedback}</p> : null}<div className="mt-4 grid gap-2"><button type="button" onClick={save} disabled={pending || (document.warnings.length > 0 && !acknowledgeWarnings)} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white px-4 text-sm font-bold disabled:opacity-40"><Save className="h-4 w-4" />Save review</button><button type="button" onClick={approve} disabled={!canApprove || pending || document.warnings.length > 0 || !allSelectedValid || document.status === "ready" || document.status === "routed"} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-lg bg-emerald-700 px-4 text-sm font-bold text-white disabled:opacity-40"><CheckCircle2 className="h-4 w-4" />{document.status === "ready" || document.status === "routed" ? "Approved" : "Approve document"}</button><button type="button" onClick={retry} disabled={pending} className="inline-flex min-h-10 items-center justify-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-bold"><RotateCw className="h-3.5 w-3.5" />Re-read with AI</button>{pending ? <p className="flex items-center justify-center gap-2 text-xs font-semibold text-[#0071e3]"><LoaderCircle className="h-4 w-4 animate-spin" />Working…</p> : null}</div></section>
      <section className="border border-slate-200 bg-white p-4 shadow-sm"><h2 className="font-bold">Choose destination</h2><p className="mt-1 text-xs leading-5 text-slate-500">Routing becomes available only after approval.</p><div className="mt-4 grid gap-2">{supplierDocument ? <button type="button" onClick={routeSupplier} disabled={!canApprove || pending || !["ready", "routed"].includes(document.status)} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-lg bg-[#0071e3] px-4 text-sm font-bold text-white disabled:opacity-40"><Route className="h-4 w-4" />Supplier pricing / catalog</button> : null}<Link href="/owner/materials/requests" className="inline-flex min-h-10 items-center justify-center rounded-lg border border-slate-300 px-3 text-xs font-bold">Open material requests</Link><Link href="/admin/ai-tools/estimate-converter" className="inline-flex min-h-10 items-center justify-center rounded-lg border border-slate-300 px-3 text-xs font-bold">Open client estimate tool</Link></div><p className="mt-3 text-[11px] leading-4 text-slate-500">Client/project linking remains a separate confirmation so AI cannot attach a document to the wrong customer.</p></section>
      {documentUrl ? <details className="overflow-hidden border border-slate-200 bg-white shadow-sm"><summary className="cursor-pointer px-4 py-4 font-bold">Original document</summary><div className="border-t border-slate-200"><a href={documentUrl} target="_blank" rel="noreferrer" className="m-3 inline-flex min-h-10 items-center gap-2 rounded-lg border border-slate-300 px-3 text-sm font-bold">Open full size <ExternalLink className="h-4 w-4" /></a>{document.mime_type.startsWith("image/") ? <Image src={documentUrl} alt={`Original ${document.file_name}`} width={1000} height={1400} unoptimized className="max-h-[34rem] w-full object-contain p-3 pt-0" /> : <iframe src={documentUrl} title={`Original ${document.file_name}`} className="h-[30rem] w-full border-0" />}</div></details> : <div className="border border-amber-200 bg-amber-50 p-4 text-xs font-semibold text-amber-900">The original is stored, but a temporary preview link could not be created. Reload to try again.</div>}
    </aside>
  </div>
}
