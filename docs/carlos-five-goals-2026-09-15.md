# My 5 Goals — completion scope

Owner requested finishing the ten-option, five-selected-goal board on Carlos's existing `/admin/daily-summary` page. No daily/weekly cadence was selected: goals persist until finished, with no automatic reset. This was disclosed before implementation.

- Ten approved outcome options; select up to five. Each has notes, related URL, In progress/Done/Blocked. Five supplier quotes are one goal with an internal 0–5 count. Done requires notes/link and all five receipts for that goal. These are reported outcomes, not independently verified supplier events or automatic owner approvals.
- Changing selection preserves notes for unselected options. No replacement of working-guide, payroll or attendance controls. No salary/payment changes, provider sends, client quote approvals or vendor outreach.
- Debounced serialized autosave, actual server revision CAS, lost-response idempotence, error/Retry, tab recovery requiring review, before-unload warning, explicit confirmed discard/reload after conflict. Text/URL drafts save even while incomplete; only completion requires evidence. No default selections or fake accomplishments.
- Private singleton table, RLS and no client table DML; invoker public RPC wrappers call narrowly authenticated private implementation using existing exact-email+role+active+approved helper. Only Carlos and David can access the board. No service keys used by the frontend.
- Base production `2fa13db8a074a239286212164ce450eb2fdf537f`; isolated `/tmp/avantia-carlos-five-goals-20260915`, branch `codex/carlos-five-goals-20260915`.
- Local migration `20260915155526_carlos_five_goals.sql`, SHA256 `5e5db2f513a50a8448cecdabe3d2fb564439c02aeb3193572967f364cee46806`; applied to verified production `nprfhspwdflpqlopydmp` as migration `20260915160403` after isolated PostgreSQL17 validation. Blank board, no attendance mutations. Security advisors reviewed: private deny-all RLS informational finding expected; existing unrelated warnings not changed.
- PostgreSQL17 script tests actual migration: permitted Carlos/owner reads/writes, max-five, stale conflict, identical retry, result/receipt validation; anon, other staff, inactive staff and direct DML denied.
- 24 Chromium/WebKit checks pass including 390/1440 board, autosave/reload, simulated network retry/tab recovery, conflicting external edit preserved, work guide and payroll regressions. Initial fixture locator errors corrected (stable summary locator and explicit Status aria-label). Build 153 routes, typecheck and scoped lint passed; latest tests are source/fixture-only changes after build.
- Live post-release verification and exact deployed SHA recorded in master context. Scoped live QA must start from blank board, change only goal0 with a temporary QA note, verify reload, then restore no selections/no notes. Never click attendance/payment actions; non-goal POSTs blocked in owned CDP9222 page.
