import type { Metadata } from "next";
export { default } from "@/app/not-found";
export const metadata: Metadata = {
  title: "Page unavailable | Avantia Build",
  robots: { index: false, follow: false },
};
